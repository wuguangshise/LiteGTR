# Recipe ablation: the default box loss is GIoU 2.0 (GFL's own). This is the CIoU 2.0 +
# NWD 1.0 loss used briefly as the default, which did worse.
_base_: [../models/model_main.yaml]
loss:
  iou_type: ciou
  nwd_weight: 1.0
